"""Image generation processor implementation.

This module provides a dedicated processor for text-to-image generation tasks
using OpenAI-compatible HTTP backends (e.g., SGLang diffusion servers). It can emit either saved image paths or in-memory
PIL images.
"""
