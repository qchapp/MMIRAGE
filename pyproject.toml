[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "anonlib"
version = "0.1.4"
description = "Declarative, distributed dataset processing with generative models."
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
  "transformers==5.3.0",
  "pyzmq==27.1.0",
  "uvloop==0.21.0; platform_system != 'Windows'",
  "fastapi==0.136.1",
  "partial-json-parser==0.2.1.1.post7",
  "sentencepiece==0.2.1",
  "msgspec==0.21.1",
  "nest-asyncio==1.6.0",
  "pyyaml==6.0.3",
  "json-repair==0.59.8",
  "tqdm==4.67.3",
  "datasets==4.8.5",
  "pyarrow==24.0.0",
  "huggingface-hub==1.14.0",
  "numpy==2.3.5",
  "fsspec==2026.2.0",
  "dacite==1.9.2",
  "pydantic==2.13.4",
  "jmespath==1.1.0",
  "jinja2==3.1.6",
  "pillow==12.1.1",
  "typing-extensions==4.15.0",
  "humanize==4.15.0",
  "openai==2.6.1",
  "pebble==5.2.0",
]

[project.optional-dependencies]
gpu = [
  "sglang==0.5.10",
  "sgl-kernel==0.3.21",
  "xgrammar==0.1.32",
  "compressed-tensors==0.15.0.1",
]

dev = [
  "ruff>=0.16",
  "pre-commit>=4.0",
  "mypy>=1.8.0",
  "ipykernel",
  "pytest",
]
image_gen = [
  "sglang[diffusion]==0.5.10",
]

[project.scripts]
anonlib = "anonlib.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/anonlib"]

[tool.hatch.build.targets.sdist]
include = ["src/anonlib/**", "pyproject.toml", "README.md"]

[tool.ruff]
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# Sphinx conf.py adjusts sys.path before imports
"docs/conf.py" = ["E402"]
# This comparison runner selects its isolated framework environment before
# importing optional NeMo/Data Designer dependencies.
"experiments/nemo_curator_comparison/scripts/run_nemo_curator_pipeline.py" = ["E402"]
