{
  "model_id": "Qwen/Qwen2.5-VL-7B-Instruct",
  "model_revision": "RECORD_WITH: hf models info Qwen/Qwen2.5-VL-7B-Instruct --revision main",
  "server": "sglang",
  "server_version": "0.5.10 recommended to match anonlib pyproject optional gpu extra",
  "openai_base_url": "http://127.0.0.1:30000/v1",
  "port": 30000,
  "tp_size": 1,
  "dtype": "bfloat16",
  "temperature": 0.0,
  "top_p": 1.0,
  "max_tokens": 128,
  "concurrency": 64,
  "notes": [
    "Use the same externally managed OpenAI-compatible SGLang server for both framework clients when possible.",
    "If either stock client cannot use this endpoint in the local environment, record the mismatch in each run manifest and do not interpret throughput as pure framework overhead."
  ]
}
